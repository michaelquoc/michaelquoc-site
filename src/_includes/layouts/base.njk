<!DOCTYPE html>
<html lang="en">
<head>
    {# --- SEO LOGIC ENGINE --- #}
    {% set seoEntry = seo_ledger[page.fileSlug] %}
    {% set finalTitle = seo_title or seoEntry.title or title or "Michael Quoc" %}
    {% set finalDesc = seoEntry.description or description or "System Architect" %}
    
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ finalTitle }}</title>
    <meta name="description" content="{{ finalDesc }}">
    
    <meta property="og:title" content="{{ finalTitle }}">
    <meta property="og:description" content="{{ finalDesc }}">
    
    <!-- Favicon -->
    <link rel="icon" href="/favicon.ico" sizes="32x32">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <link rel="stylesheet" href="/style.css?v=3">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=JetBrains+Mono:wght@400;500&family=Source+Serif+4:opsz,wght@8..60,300;8..60,400;8..60,600&display=swap" rel="stylesheet">
    <script defer data-domain="michaelquoc.com" src="https://plausible.io/js/script.js"></script>


</head>
<body>

    {% include "components/header.njk" %}

    <main class="container">
        {{ content | safe }}
    </main>

    {% include "components/footer.njk" %}

</body>
</html>